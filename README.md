# ViveFacilBack
python 3.8
# crea un entorno al nivel del archivo manage.py
>py -3.8 -m venv venv
>python -m venv venv
# activalo
>venv\Scripts\activate.bat
# actualiza pip
>python -m pip install --upgrade pip
# instala dependencias
>pip install -r requirements.txt
# corre el programa
>python manage.py runserver